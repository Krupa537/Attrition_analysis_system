# Email Configuration Template
# Copy this file and rename to .env (add .env to .gitignore)
# Or set these as environment variables in your system

# SMTP Server Configuration
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587

# Email Credentials (Required for sending alerts)
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password-here

# Sender Email (defaults to SMTP_USERNAME if not set)
SENDER_EMAIL=your-email@gmail.com

# Instructions:
# 1. For Gmail:
#    - Enable 2-Step Verification in your Google Account
#    - Generate an App Password: https://myaccount.google.com/apppasswords
#    - Use the 16-character app password as SMTP_PASSWORD
#
# 2. For Outlook/Office365:
#    - SMTP_SERVER=smtp.office365.com
#    - Use your regular email credentials
#
# 3. For Yahoo:
#    - SMTP_SERVER=smtp.mail.yahoo.com
#    - Generate an app password in Yahoo Account Security
